import React from 'react';
import { Hanze } from '../../../pages/Hanze';

// Interactive Hanzehogeschool workshop wizard (multi-step useState flow). noindex,
// renders its own footer. Fully client-side; SEO meta baked in the .astro page.
export default function HanzeIsland() {
  return <Hanze />;
}
